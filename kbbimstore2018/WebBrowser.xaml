<Page x:Class="KbBimstore.KBRevitWebBrowser"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:dat="clr-namespace:System.Windows.Data;assembly=PresentationFramework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        Title="Dockable Dialogs"  Height="500" Width="200"
        x:Name="DockableDialogs"  Background="LightGray" Loaded="DockableDialogs_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="35"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtAddressBar" Height="30"  Margin="5" VerticalContentAlignment="Center" KeyDown="txtAddressBar_KeyDown"/>
            <Button x:Name="btnNavigate" Grid.Column="1" Margin="0,5,5,5" Content="Go" Click="btnNavigate_Click"/>
        </Grid>
        <WebBrowser Grid.Row="1" x:Name="webBrowser" Margin="5,0,5,5"/>

    </Grid>
</Page>
